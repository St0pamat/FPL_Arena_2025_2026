# Placeholder — herby klubów (opcjonalny fallback)

Pliki PNG: `{slug}.png`, np. `manchester-united.png`, `arsenal.png`.

Hub najpierw bierze logo z `/club-logos/` (panel admina), potem próbuje `/images/clubs/{slug}.png`.
